let coder = 'I am a good programmer'
let stringType = typeof(coder)
let stringLen = coder.length

let toUpper = coder.toUpperCase()
let toLower = coder.toLowerCase()
let stringSearch = coder.includes('good')
let stripWord = coder.replace('programmer', '')
let findWord = coder.endsWith('programmer');
let splitString = coder.split('good')